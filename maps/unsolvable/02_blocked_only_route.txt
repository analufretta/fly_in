# Unsolvable: the single path to the end runs through a blocked zone, which the
# solver removes from the graph, severing the only route.
# Expect: parses OK, then SolveError (zero flow), exit 1.
nb_drones: 2

start_hub: start 0 0 [color=green]
hub: wall 1 0 [zone=blocked color=gray]
end_hub: goal 2 0 [color=red]

connection: start-wall
connection: wall-goal
