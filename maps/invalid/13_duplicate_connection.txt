# Invalid: the same link declared twice, order-independent (a-b then b-a).
# Expect MapError "duplicated connection", exit 1.
nb_drones: 1

start_hub: a 0 0 [color=green]
end_hub: b 1 0 [color=red]

connection: a-b
connection: b-a
